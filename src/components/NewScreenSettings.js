import React from 'react';
import { Button } from 'react-native';
import { TouchableOpacity } from 'react-native-gesture-handler';

export const NewScreenSettings = () => {

  return (
    <TouchableOpacity>
      <Button title='...' />
    </TouchableOpacity>
  )
}
